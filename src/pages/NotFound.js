import React from 'react';
import UseTitle from './../customHooks/UseTitle';

const NotFound = () => {
    
    UseTitle('404');
    return (
        <div>
            
        </div>
    );
};

export default NotFound;